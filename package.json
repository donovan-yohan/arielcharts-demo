{
  "name": "arielcharts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter 'apps/*' run dev",
    "build": "pnpm --filter 'apps/*' run build"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
