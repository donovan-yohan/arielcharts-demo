schema_version: "1"
description: "QA — validates implementation against spec via browser and CLI testing"
vendor: opencode
model: kimi-k2.5
max_turns: 30
max_duration: "30m"
ephemeral: true
workspace: inherit
belayer_tools: []
